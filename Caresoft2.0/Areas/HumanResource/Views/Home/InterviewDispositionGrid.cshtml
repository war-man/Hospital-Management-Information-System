@model IEnumerable<CaresoftHMISDataAccess.HRApplication>


<table class="table table-data table-condensed">

    <tr>
       <th>Edit</th>
        <th>Delete</th>
        <th>
            @Html.DisplayNameFor(model => model.DispositionReasonId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DispositionDescription)
        </th>

    <tbody class="bg-white">
        @foreach (var item in Model.OrderByDescending(e => e.Id))
        {
            <tr>
                <td>Edit</td>
                <td>Delete</td>
                <td>
                    @Html.DisplayFor(modelItem => item.DispositionReasonId)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.DispositionDescription)
                </td>

            </tr>
        }
    </tbody>
</table>




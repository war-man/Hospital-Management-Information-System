@model IEnumerable<CaresoftHMISDataAccess.HRApplication>


<table class="table table-data table-condensed">

    <tr>
        
        <th>
            @Html.DisplayNameFor(model => model.SourceCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SourceDesription)
        </th>

    <tbody class="bg-white">
        @foreach (var item in Model.OrderByDescending(e => e.Id))
        {
            <tr>

                <td>
                    @Html.DisplayFor(modelItem => item.SourceCode)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.SourceDesription)
                </td>

            </tr>
        }
    </tbody>
</table>



